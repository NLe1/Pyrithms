# .githooks.ini

[pre-commit]
command = pytest && mypy --ignore-missing-imports . && black --check . || true

[pre-push]
command = pytest --doctest-modules && flake8 && coverage run -m unittest discover && pytest --doctest-modules
